// Log In / Get Started intentionally have no href yet — the production
// TestDart app URL isn't finalized, and a fake/.example link would look
// functional without being one. They render as static buttons until a
// real URL exists; wire it up here when it does.
export const CONTACT_EMAIL = 'info@provassuretech.com';
